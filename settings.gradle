pluginManagement {
    repositories {
        maven {
            name = "Fabric"
            url = "https://maven.fabricmc.net/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "I Want To Control My Server"

include(
        "server",
        "fabric",
        "front",
        "static",
        "cli"
)
